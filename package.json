{
  "name": "coolright",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "migrate": "node db/migrate.js",
    "test": "node --test --test-concurrency=1 --experimental-test-module-mocks \"test/*.test.js\"",
    "check:emdash": "node scripts/check-no-em-dash.js",
    "check": "npm run check:emdash && npm test"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@node-rs/argon2": "^2.0.2",
    "@vercel/blob": "^0.27.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
